"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var date_fns_1 = require("date-fns");
function DateString(item, args) {
    if (typeof item == "string" && item.length > 4 && date_fns_1.isValid(new Date(item))) {
        var formatString = args == "short" ? "MMM D" : "MMM D, YYYY";
        return date_fns_1.format(new Date(item), formatString);
    }
    else {
        return item;
    }
}
exports.DateString = DateString;
//# sourceMappingURL=date-string.js.map