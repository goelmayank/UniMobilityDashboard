import { HtBaseApi } from "./base";
import { Observable } from "rxjs/Observable";
import { IAction } from "ht-models";
export declare class HtTrackingApi extends HtBaseApi {
    name: string;
    trackType: {
        shortCode: string;
        lookupId: string;
        collectionId: string;
        actionId: string;
    };
    constructor(request: any);
    track(id: string, idType?: 'shortCode' | 'lookupId' | 'collectionId' | 'actionId', token?: string): Observable<IAction[]>;
}
