import { Observable } from "rxjs/Observable";
import { HtRequest } from "../core/request";
export declare class HtBaseApi {
    request: HtRequest;
    base: string;
    constructor(request: HtRequest, base: string);
    get<T>(id: string, query?: {}, token?: string): Observable<T>;
    index<T>(query?: {}, token?: string): Observable<T>;
    summary<T>(query?: {}, token?: string): Observable<T>;
    heatmap<T>(query?: {}, token?: string): Observable<T>;
    api$<T>(path: any, query?: {}, options?: {
        isAdmin?: boolean;
        token?: string;
    }): Observable<T>;
    postApi$<T>(path: string, body: any, options?: {
        isAdmin?: boolean;
        token?: string;
    }): Observable<T>;
    placeline<T>(id: any, query?: {}, token?: string): Observable<T>;
    allPages<T = any>(api$: any, options?: {
        isAdmin?: boolean;
        token?: string;
    }): any;
    analytics(query: any): Observable<any>;
}
