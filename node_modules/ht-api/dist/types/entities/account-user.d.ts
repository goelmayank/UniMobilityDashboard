import { HtBaseApi } from "./base";
import { Observable } from "rxjs/Observable";
import { IAccountUser } from "ht-models";
import { HtRequest } from "../core/request";
export declare class HtAccountUserApi extends HtBaseApi {
    name: string;
    constructor(request: HtRequest);
    login(user: {
        username: string;
        password: string;
    }): Observable<IAccountUser>;
    get<IAccountUser>(id: any, token: any): Observable<IAccountUser>;
    memberships(id: any, query?: {}, options?: object): Observable<{}>;
    membershipsAll(id: any, query: any, token?: string): any;
}
