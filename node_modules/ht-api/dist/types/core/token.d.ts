export declare class HtToken {
    _token: string;
    _tempToken: string;
    _groupToken: string;
    constructor(token?: string);
    token: string;
    tempToken: string;
    groupToken: string;
    readonly currentToken: string;
}
