import { ReplaySubject } from "rxjs/ReplaySubject";
import { Observable } from "rxjs/Observable";
import { BehaviorSubject } from "rxjs/BehaviorSubject";
export declare class EntityState<S = any> {
    stateSubject$: BehaviorSubject<object>;
    state$: Observable<object>;
    dataSubject$: ReplaySubject<S[]>;
    data$: Observable<S[]>;
    stateTrasfrom: {
        selector: string;
        map(data, state): any;
    }[];
    constructor();
    setState(selector: any, value: any): void;
    addState(selector: any, map: any): void;
    selector(selector: any): Observable<any>;
    setData(data: S[]): void;
}
