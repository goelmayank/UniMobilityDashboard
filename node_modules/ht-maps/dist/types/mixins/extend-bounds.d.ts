import { Constructor, Entities } from "../interfaces";
import { HtBounds } from "ht-map-wrapper";
import { MapInstance } from "../map-utils/map-instance";
export interface IExtendBoundsBase {
    getBounds: (item, bounds: HtBounds) => HtBounds;
    mapInstance: MapInstance;
}
export declare function ExtendBoundsMixin<TBase extends Constructor<IExtendBoundsBase>>(Base: TBase): {
    new (...args: any[]): {
        entities: Entities<any>;
        toIncludeInBounds: boolean;
        extendBounds(bounds: any): any;
        getBounds: (item: any, bounds: any) => any;
        mapInstance: MapInstance;
    };
} & TBase;
