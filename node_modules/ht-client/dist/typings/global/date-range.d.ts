import { IDateRange } from "../interfaces";
import { BehaviorSubject } from "rxjs/BehaviorSubject";
import { Observable } from "rxjs/Observable";
export declare const defaultDateRange: {
    start: any;
    end: any;
};
export declare class DateRange {
    dataBehaviour$: BehaviorSubject<IDateRange>;
    data$: Observable<IDateRange | null>;
    constructor(initialDate?: Partial<IDateRange>);
    setDateRange(dateRange?: IDateRange | null): void;
    getDateRange(): IDateRange;
    readonly display$: Observable<string>;
}
export declare const dateRangeFactory: (initialDate?: Partial<IDateRange>) => DateRange;
export declare const dateRangeService: {
    getInstance(initialDate?: IDateRange): DateRange;
};
