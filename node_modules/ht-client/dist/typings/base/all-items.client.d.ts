import { EntityListClient } from "./list-client";
import { Observable } from "rxjs/Observable";
import { AllData } from "ht-models";
export declare abstract class EntityAllItemsClient extends EntityListClient {
    updateStrategy: string;
    allowedQueryKeys: string[];
    abstract dataEntities$: Observable<AllData<any>>;
    constructor();
    readonly data$: Observable<AllData<any> | {
        count: number;
        results: any;
        previous: string;
        next: string;
    }>;
    getDefaultQuery(): object;
    abstract setLoading(data: any): any;
    firstDataEffect(data: any): void;
    readonly dataArray$: Observable<any>;
    getAllMarkers$(): Observable<any>;
    isValidMarker(marker: any): boolean;
    getMarkers$(): Observable<{}>;
}
