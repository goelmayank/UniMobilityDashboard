import { Observable } from "rxjs/Observable";
export declare abstract class EntityClient {
    dataArrayWithSelected$(id$: any, dataArray$: any, selected$: any): Observable<any>;
    pageDataWithSelected$(id$: any, pageData$: any, selected$: any): Observable<{
        results: any;
        count: number;
        next: string;
        previous: string;
    }>;
    getPageFromEntity(item$: any): any;
}
