var HtStop = (function () {
    function HtStop(data) {
        this.data = data;
    }
    HtStop.prototype.getPosition = function () {
        var pos = null;
        if (this.data.location && this.data.location.geojson.coordinates) {
            pos = this.data.location.geojson.coordinates;
            return { lat: pos[1], lng: pos[0] };
        }
        else {
            return null;
        }
    };
    return HtStop;
}());
export { HtStop };
export var htStop = function (action) { return new HtStop(action); };
//# sourceMappingURL=stop.js.map