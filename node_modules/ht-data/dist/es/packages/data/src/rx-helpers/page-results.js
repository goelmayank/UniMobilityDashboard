import { map } from "rxjs/operators";
export var PageResults$ = function (pageData$) {
    return pageData$.pipe(map(function (pageDate) {
        return pageDate ? pageDate.results : pageDate;
    }));
};
//# sourceMappingURL=page-results.js.map