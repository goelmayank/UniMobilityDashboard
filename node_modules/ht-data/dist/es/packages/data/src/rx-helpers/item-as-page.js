import { map } from "rxjs/operators";
export var itemAsPage$ = function () {
    return function (item$) {
        return item$.pipe(map(function (item) {
            return item
                ? {
                    count: 1,
                    next: null,
                    previous: null,
                    results: [item]
                }
                : null;
        }));
    };
};
//# sourceMappingURL=item-as-page.js.map