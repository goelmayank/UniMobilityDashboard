"use strict";
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var operators_1 = require("rxjs/operators");
var empty_1 = require("rxjs/observable/empty");
function allPages(api$, req$, options) {
    if (options === void 0) { options = {}; }
    return api$.pipe(operators_1.expand(function (data) {
        return data["next"]
            ? req$(data["next"], options).pipe(operators_1.map(function (newData) {
                return __assign({}, newData, { results: data.results.concat(newData.results) });
            }))
            : empty_1.empty();
    }));
}
exports.allPages = allPages;
//# sourceMappingURL=all-pages.js.map