import { Observable } from "rxjs/Observable";
import { Page, AllData } from "ht-models";
export declare const PageResults$: (pageData$: Observable<Page<any> | AllData<any>>, defaultValue?: any) => Observable<any>;
