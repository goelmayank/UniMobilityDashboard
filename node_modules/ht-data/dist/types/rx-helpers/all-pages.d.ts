import { Page } from "ht-models";
import { Observable } from "rxjs/Observable";
export declare function allPages<T = any>(api$: Observable<Page<T>>, req$: (url: string, options: object) => Observable<Page<T>>, options?: {}): Observable<Page<T>>;
