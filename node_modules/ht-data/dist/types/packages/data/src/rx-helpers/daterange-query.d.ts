import { Observable } from "rxjs/Observable";
import { IDateRange } from "ht-models";
export declare const DateRangeToQuery$: (dateRangeParam: string) => (param: Observable<IDateRange>) => Observable<object>;
