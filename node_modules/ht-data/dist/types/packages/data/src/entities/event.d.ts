import { IEvent } from "ht-models";
import { IEventDisplay } from "../interfaces";
export declare class HtEvent {
    data: IEvent;
    constructor(data?: IEvent);
    getEventDisplay(): IEventDisplay;
}
export declare const htEvent: (event?: IEvent) => HtEvent;
