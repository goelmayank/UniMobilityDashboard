import { ISegment } from "ht-models";
export declare class HtStop {
    data: ISegment;
    constructor(data?: ISegment);
    getPosition(): {
        lat: any;
        lng: any;
    };
}
export declare const htStop: (action?: ISegment) => HtStop;
