import { OnInit } from '@angular/core';
import { IAction } from "ht-models";
export declare class DestinationPopupComponent implements OnInit {
    action: IAction;
    constructor();
    ngOnInit(): void;
    getDestinationName(action: IAction): string;
}
