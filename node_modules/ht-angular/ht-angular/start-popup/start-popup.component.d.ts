import { OnInit } from '@angular/core';
import { IAction } from "ht-models";
export declare class StartPopupComponent implements OnInit {
    action: IAction;
    constructor();
    ngOnInit(): void;
    getStartName(action: IAction): string;
}
