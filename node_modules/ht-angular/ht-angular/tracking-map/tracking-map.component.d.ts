import { OnInit } from '@angular/core';
import { HtMapService } from "../ht/ht-map.service";
import { TrackingMapService } from "./tracking-map.service";
import { BehaviorSubject } from "rxjs/BehaviorSubject";
export declare class TrackingMapComponent implements OnInit {
    private mapService;
    trackingMapService: TrackingMapService;
    mapInstance: any;
    showSummary: boolean;
    card: any;
    constructor(mapService: HtMapService, trackingMapService: TrackingMapService);
    readonly dirty$: BehaviorSubject<boolean>;
    readonly setBoundsOptions: {
        paddingBottomRight: number[];
        paddingTopLeft: number[];
        duration: number;
        animate: boolean;
        easeLinearity: number;
    };
    ngOnInit(): void;
}
