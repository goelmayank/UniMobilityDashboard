import { HtMapService } from "../ht/ht-map.service";
import { BehaviorSubject } from "rxjs/BehaviorSubject";
import { Subscription } from "rxjs/Subscription";
import { MapInstance } from "ht-maps";
export declare class TrackingMapService {
    mapService: HtMapService;
    checkDirtySub: Subscription;
    mapInstance: MapInstance;
    dirty$: BehaviorSubject<boolean>;
    actionCompleted: boolean;
    defaultSetBoundsOptions: {
        paddingTopLeft: number[];
        paddingBottomRight: number[];
        duration: number;
        animate: boolean;
        easeLinearity: number;
    };
    setBoundsOptions: {
        paddingTopLeft: number[];
        paddingBottomRight: number[];
        duration: number;
        animate: boolean;
        easeLinearity: number;
    };
    actionsTrace: any;
    destination: any;
    polyline: any;
    popups$: any;
    userPopup$: any;
    startPopup$: any;
    constructor(mapService: HtMapService);
    resetCleanMap(action: any): void;
    setData$(actionsData$: any): void;
    resetMap(): void;
    onComplete(action: any): void;
    setStyle(): void;
    getUserIconDiv(action: any, bearing: any, size: any): string;
}
