import { IAction } from "ht-models";
import { ReplaySubject } from "rxjs/ReplaySubject";
export declare class TrackingService {
    trackActions: any;
    isMapDirty: boolean;
    isSliding: boolean;
    trackApi: any;
    actions$: ReplaySubject<IAction[]>;
    error$: ReplaySubject<any | null>;
    constructor();
    setTrackApi(): void;
    initShortCode(shortCode: string): void;
    handleOnReady(actions: IAction[]): void;
    handleOnUpdate(actions: IAction[]): void;
    handleOnError(err: any | null): void;
    private handleDeepLinkRedirect(subAccount, action);
}
