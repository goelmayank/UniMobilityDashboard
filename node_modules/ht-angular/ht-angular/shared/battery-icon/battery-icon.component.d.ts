import { OnInit } from '@angular/core';
export declare class BatteryIconComponent implements OnInit {
    battery: number;
    layout: 'column' | 'row';
    reverse: boolean;
    iconType: 'fa' | 'ionicon';
    constructor();
    ngOnInit(): void;
    batteryClass(level: number): string;
    private faBatteryClass(level);
    private ioniconBatteryClass(level);
}
